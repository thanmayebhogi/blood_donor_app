<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F9F9F9">

    <!-- Header Bar -->
    <View
        android:id="@+id/headerBg"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="#D32F2F"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textViewEmergency"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EMERGENCY REQUESTS"
        android:textColor="@color/material_dynamic_secondary20"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/headerBg"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/headerBg" />

    <!-- Actual List for Emergency Data -->
    <ListView
        android:id="@+id/listViewEmergency"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="10dp"
        android:divider="#EEEEEE"
        android:dividerHeight="1dp"
        app:layout_constraintBottom_toTopOf="@+id/btnPostUrgent"
        app:layout_constraintTop_toBottomOf="@id/headerBg" />

    <!-- Button to open the Posting Form -->
    <Button
        android:id="@+id/btnPostUrgent"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#D32F2F"
        android:text="POST URGENT REQUEST"
        android:textColor="#FFFFFF"
        app:layout_constraintBottom_toTopOf="@+id/btnHomeFromEmergency"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Button: Back to Home -->
    <Button
        android:id="@+id/btnHomeFromEmergency"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="#333333"
        android:text="BACK TO HOME"
        android:textColor="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>